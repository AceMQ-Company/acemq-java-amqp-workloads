target/
.flattened-pom.xml
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar
.idea/
*.iml
.vscode/
.settings/
.classpath
.project
.DS_Store
*.log
.env
site/

# Generated by maven-shade-plugin during package. Tracking it means it churns on
# every build and disagrees with pom.xml until somebody happens to rebuild.
dependency-reduced-pom.xml

# The front end's build output and its dependencies. The bundle is built into
# the jar by Maven, so a committed dist/ would be a second copy that drifts.
studio/src/main/frontend/node_modules/
studio/src/main/frontend/dist/
